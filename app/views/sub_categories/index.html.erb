<h1>Listing sub_categories</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Description</th>
    <th>Category</th>
  </tr>

<% @sub_categories.each do |sub_category| %>
  <tr>
    <td><%=h sub_category.name %></td>
    <td><%=h sub_category.description %></td>
    <td><%=h sub_category.category.name%></td>
    <td><%= link_to 'Show', sub_category %></td>
    <td><%= link_to 'Edit', edit_sub_category_path(sub_category) %></td>
    <td><%= link_to 'Destroy', sub_category, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New sub_category', new_sub_category_path %>